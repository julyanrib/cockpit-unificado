# QUE COMANDO CADA TABELA AUTORIZA — o contrato que a guarda 24 cobra do front.
#
# POR QUE ESTE ARQUIVO EXISTE: sem política, o Postgres NÃO dá erro. Um select sem
# política devolve `[]` e a tela mostra "não tem dado". Um delete sem política apaga
# zero linhas e devolve sucesso, e a tela diz que apagou. Foi assim que dois botões
# de apagar passaram meses mentindo (pdi_documentos e playbook_progresso, medidos no
# banco com transação e rollback em 07/09/26).
#
# A guarda `checar-politica-do-front.js` lê este arquivo e o template, e reprova o
# build quando o front usa um comando que a tabela não autoriza. Ela NÃO toca no
# banco — build tem que rodar offline, no CI, sem credencial.
#
# COMO ATUALIZAR: `node scripts/ler-politicas-do-banco.js --sql`, rode a consulta,
# e passe o JSON de volta para este script. Nunca edite à mão: à mão este arquivo
# passa a descrever o que eu queria e não o que o banco faz.
#
# `(nenhuma)` é legítimo em 11 tabelas, lidas SÓ
# pelo servidor (api/, com service_role): _backup_planos_semanais_app_20260924, _update_webhook_backfill_queue, cockpit_config, cockpit_snapshot, etapa_corrigida_backup, geocode_repair_attempts, geocode_repair_cron_state, novidades_mercado, plano_rota_sincronizado, restaurantes_osm, webhook_cooldown.
# RLS ligada com zero política é exatamente a proteção que elas precisam —
# cockpit_snapshot, por exemplo, guarda o CRM inteiro. Se o front passar a ler
# qualquer uma delas, a guarda 24 reprova, e a resposta certa é uma rota em api/,
# nunca uma política de leitura aqui.
#
# gerado por scripts/ler-politicas-do-banco.js em 26/09/2026

_backup_planos_semanais_app_20260924 = (nenhuma)
_update_webhook_backfill_queue = (nenhuma)
analise_individual_mensal = select
analise_individual_semanal = select
app_force_reload = select,update
audit_logs = select
client_meetings = select,insert,update,delete
client_notes = select,insert,update,delete
client_stage_changes = select,insert
client_status_history = select
client_statuses = select,insert,update,delete
client_tasks = select,update
client_visits = select
clients = select,insert,update,delete
cockpit_config = (nenhuma)
cockpit_snapshot = (nenhuma)
combinados_cumprimento = select,insert,update,delete
combinados_semana = select,insert,update,delete
comunicados = select,insert,update,delete
comunicados_lidos = select,insert
dailies = select,insert,update
equipe_cockpit = select,insert,update,delete
etapa_corrigida_backup = (nenhuma)
etapa_de_para = select,insert,update,delete
field_route_stops = select,insert,update,delete
field_routes = select,insert,update,delete
fila_pwa = select,insert,update,delete
geocode_repair_attempts = (nenhuma)
geocode_repair_cron_state = (nenhuma)
import_batches = select,insert
import_batches_log = select,insert,update,delete
import_logs = select
leads_prospeccao = select,insert,update
modos_de_agir = select,insert,update,delete
noticias_setor = select
novidades_mercado = (nenhuma)
pauta_do_lider = select,insert,update,delete
pdi_compromissos = select,insert,update
pdi_documentos = select,insert,delete
perfis = select,insert,update
plano_rota_sincronizado = (nenhuma)
planos_diarios = select,insert,update
planos_semanais = select,insert,update
playbook_copias = select,insert
playbook_progresso = select,insert,update
profiles = select,insert,update,delete
radar_pracas = select
registros_rodada = select,insert,update,delete
restaurantes_osm = (nenhuma)
resumos_ia = select
route_config = select,update
sector_visibility = select,insert,update,delete
seller_classification = select,insert,update,delete
seller_visit_goals = select,insert,update,delete
snapshot_farol = select
stage_change_failures = select,insert
stage_property_options = select
stage_sla = select,insert,update,delete
sugestoes_planos = select,insert
target_accounts = select
um_a_um = select,insert,update,delete
um_a_um_audio = select,insert,update,delete
um_a_um_documentos = select,insert,delete
vendedor_apelido = select,insert,update,delete
webhook_cooldown = (nenhuma)
