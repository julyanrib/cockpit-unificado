{
  "name": "cockpit-unificado",
  "version": "1.0.0",
  "private": true,
  "description": "Cockpit Unificado — HubSpot + Expogo, auto-atualizado diariamente via GitHub Actions",
  "scripts": {
    "fetch": "node scripts/fetch-hubspot.js",
    "build": "node scripts/build.js",
    "refresh": "npm run fetch && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}
